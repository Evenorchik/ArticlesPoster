# ArticlesPoster

Автоматизированная система для создания, обработки и публикации SEO-оптимизированных статей на Medium.com с использованием AI, PyAutoGUI и Ads Power.

## 📋 Описание

ArticlesPoster - это комплексная система, которая:
- Автоматически создает и обрабатывает статьи с помощью AI (OpenAI)
- Публикует статьи на Medium.com через PyAutoGUI
- Управляет несколькими профилями браузера через Ads Power
- Работает с PostgreSQL для хранения статей
- Отправляет уведомления через Telegram бот
- Поддерживает автоматический постинг по расписанию

## 🚀 Быстрый старт

### Требования

- Python 3.8 или выше
- Windows (для PyAutoGUI и pywin32)
- Ads Power установлен и запущен
- PostgreSQL база данных
- Chrome браузер (управляется через Ads Power)

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd ArticlesPoster
```

2. **Создайте виртуальное окружение (рекомендуется):**
```bash
python -m venv venv
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте конфигурацию:**

   Создайте файл `config.py` на основе примера:
   ```python
   # OpenAI
   OPENAI_API_KEY = "your-openai-api-key"
   OPENAI_MODEL = "gpt-5.1"
   OPENAI_MODEL_THINKING = "gpt-5.1-thinking"
   
   # PostgreSQL
   POSTGRES_DSN = "postgresql://user:password@host:port/database"
   
   # Логирование
   LOG_LEVEL = "INFO"  # или "DEBUG"
   LOG_MODE = "DEBUG"  # или "SUMMARY" для scheduled_poster
   ```

   Создайте файл `config_bot.py` для Telegram бота:
   ```python
   TELEGRAM_BOT_TOKEN = "your-telegram-bot-token"
   TELEGRAM_CHAT_ID = "your-chat-id"
   ```

5. **Настройте Ads Power:**

   - Убедитесь, что Ads Power запущен
   - Настройте профили в `poster/settings.py`:
     ```python
     PROFILE_MAPPING = {
         "profile_id_1": profile_no_1,
         "profile_id_2": profile_no_2,
         # ...
     }
     
     PROFILE_SEQUENTIAL_MAPPING = {
         profile_no_1: sequential_no_1,
         profile_no_2: sequential_no_2,
         # ...
     }
     ```

## 📖 Использование

### 1. Ручной постинг статей

Запустите основной скрипт для ручного постинга:

```bash
python poster/cli/manual_poster.py
```

Скрипт:
- Покажет список доступных таблиц из базы данных
- Попросит выбрать таблицу
- Позволит выбрать статьи для публикации (по ID или все неопубликованные)
- Автоматически откроет профили, опубликует статьи и обновит базу данных

### 2. Автоматический постинг по расписанию

Запустите планировщик для автоматического постинга:

```bash
python scheduled_poster.py
```

**Логика работы:**
- **Четные дни месяца** → использует профили 1-5
- **Нечетные дни месяца** → использует профили 6-10
- **Время постинга:** 19:30-20:20 по Киеву (случайное для каждого профиля)
- **В день:** 4 статьи с `is_link='no'` и 1 статья с `is_link='yes'`
- Для статей с `is_link='yes'` автоматически заменяет ссылку Bonza Chat на случайную с реферальным кодом

### 3. Очистка текста статей

Очистка колонки `body` от нежелательных элементов:

```bash
python clean_body_text.py
```

Удаляет:
- "Topic:" (с большой буквы)
- "topic:" (с маленькой буквы)
- "**" (две звездочки рядом)

### 4. Тестирование

**Тест открытия профилей и подготовки Medium вкладки:**
```bash
python profile_open_unit_testing.py
```

**Тест PyAutoGUI процесса постинга:**
```bash
python pyautogui_unit_testing.py
```

⚠️ **Важно:** Перед запуском `pyautogui_unit_testing.py` убедитесь, что:
- Профиль AdsPower уже открыт
- Вкладка Medium New Story открыта и активна
- Окно профиля максимизировано

## 🗂️ Структура проекта

```
ArticlesPoster/
├── poster/                    # Основной модуль постинга
│   ├── adspower/             # Управление Ads Power профилями
│   │   ├── api_client.py     # API клиент для Ads Power
│   │   ├── profile_manager.py # Менеджер профилей
│   │   ├── selenium_attach.py # Подключение Selenium
│   │   ├── tabs.py           # Управление вкладками
│   │   └── window_manager.py # Управление окнами
│   ├── clipboard/            # Работа с буфером обмена
│   │   ├── markdown_conv.py  # Конвертация Markdown
│   │   └── richtext.py       # Rich Text (CF_HTML)
│   ├── db/                   # Работа с БД
│   │   └── postgres.py       # PostgreSQL функции
│   ├── medium/               # Логика постинга на Medium
│   │   ├── poster_flow.py    # PyAutoGUI процесс постинга
│   │   └── url_fetcher.py    # Получение URL статьи
│   ├── ui/                   # UI компоненты
│   │   ├── coords.py         # Координаты для кликов
│   │   ├── interface.py      # Интерфейс UiDriver
│   │   └── pyautogui_driver.py # Реализация PyAutoGUI
│   ├── cli/                  # CLI интерфейсы
│   │   └── manual_poster.py  # Ручной постинг
│   ├── link_replacer.py      # Замена ссылок Bonza Chat
│   ├── models.py             # Модели данных
│   ├── settings.py           # Настройки и константы
│   └── timing.py             # Утилиты для задержек
├── scheduled_poster.py       # Автоматический постинг по расписанию
├── clean_body_text.py        # Очистка текста статей
├── profile_open_unit_testing.py # Тест открытия профилей
├── pyautogui_unit_testing.py   # Тест PyAutoGUI процесса
├── config.py                 # Основная конфигурация
├── config_bot.py             # Конфигурация Telegram бота
├── requirements.txt          # Зависимости Python
└── README.md                 # Этот файл
```

## ⚙️ Конфигурация

### Настройка профилей Ads Power

В файле `poster/settings.py` настройте маппинг профилей:

```python
# Маппинг profile_id -> profile_no
PROFILE_MAPPING = {
    "profile_id_1": 70,
    "profile_id_2": 74,
    # ...
}

# Последовательная нумерация профилей (1-10)
PROFILE_SEQUENTIAL_MAPPING = {
    70: 1,
    74: 2,
    # ...
}
```

### Настройка координат для PyAutoGUI

В файле `poster/ui/coords.py` настройте координаты кликов:

```python
@dataclass
class Coords:
    TITLE_INPUT: Tuple[int, int] = (633, 214)
    PUBLISH_BUTTON_1: Tuple[int, int] = (1301, 118)
    HASHTAGS_INPUT: Tuple[int, int] = (1037, 440)
    PUBLISH_BUTTON_2: Tuple[int, int] = (1051, 602)
```

⚠️ **Важно:** Координаты зависят от разрешения экрана и размера окна браузера. Используйте `click_debug_screenshots.py` для определения правильных координат.

### Настройка времени постинга

В файле `scheduled_poster.py` настройте время постинга:

```python
POSTING_START_HOUR = 19.5   # 19:30
POSTING_END_HOUR = 20.333   # 20:20
```

Время указывается по Киеву (UTC+2/UTC+3).

## 🔧 Зависимости

Все зависимости указаны в `requirements.txt`:

- **openai** - для работы с OpenAI API
- **requests** - для HTTP запросов
- **pyautogui** - для автоматизации UI
- **pyperclip** - для работы с буфером обмена
- **psycopg[binary]** - для работы с PostgreSQL
- **pygetwindow** - для управления окнами Windows
- **selenium** - для управления браузером
- **markdown** - для конвертации Markdown в HTML
- **pywin32** - для работы с Windows API
- **pytz** - для работы с часовыми поясами

## 📝 Особенности

### Замена ссылок Bonza Chat

Для статей с `is_link='yes'` система автоматически:
- Находит ссылку `https://bonza.chat/ai-girlfriend` в тексте
- Заменяет её на случайную ссылку из списка
- Добавляет реферальный параметр в зависимости от профиля

Реферальные коды настраиваются в `poster/link_replacer.py`.

### Логирование

Система поддерживает два режима логирования:
- **DEBUG** - подробное логирование всех действий
- **SUMMARY** - краткое логирование только результатов

Настраивается через `LOG_MODE` в `config.py`.

### Уведомления в Telegram

Система отправляет уведомления:
- О запуске автопостера
- О публикации каждой статьи (с URL, заголовком, хэштегами)

Настраивается через `config_bot.py`.

## 🐛 Решение проблем

### Профиль не открывается

1. Убедитесь, что Ads Power запущен
2. Проверьте правильность `profile_id` в настройках
3. Проверьте API ключ Ads Power в `poster/settings.py`

### Координаты кликов не работают

1. Используйте `click_debug_screenshots.py` для определения координат
2. Убедитесь, что окно браузера максимизировано
3. Проверьте разрешение экрана

### Ошибки подключения к БД

1. Проверьте строку подключения в `config.py`
2. Убедитесь, что PostgreSQL запущен
3. Проверьте права доступа к базе данных

### Selenium не подключается

1. Убедитесь, что профиль Ads Power активен
2. Проверьте путь к `chromedriver.exe`
3. Проверьте, что порт remote debugging доступен

## 📄 Лицензия

[Укажите лицензию, если применимо]

## 👥 Авторы

[Укажите авторов проекта]

## 🙏 Благодарности

[Укажите благодарности, если применимо]

