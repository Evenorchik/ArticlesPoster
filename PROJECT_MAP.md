# PROJECT MAP - ArticlesPoster

**FIRSTLY READ THIS** - Этот файл содержит полное описание архитектуры проекта для быстрой навигации и понимания системы.

---

## 📋 ОБЗОР ПРОЕКТА

**ArticlesPoster** - это система автоматического создания, обработки и публикации SEO-оптимизированных статей на Medium.com. Проект использует:
- AI (OpenAI, Anthropic Claude) для генерации и обработки контента
- PyAutoGUI для автоматизации браузера
- Ads Power для управления профилями браузера
- PostgreSQL для хранения статей
- Telegram бот для уведомлений
- Selenium для управления браузером

---

## 🗂️ СТРУКТУРА ФАЙЛОВ

### **КОНФИГУРАЦИЯ**

#### `config.py`
**Назначение:** Основной файл конфигурации проекта
**Содержит:**
- `OPENAI_API_KEY` - ключ API OpenAI
- `OPENAI_MODEL` - модель для структурирования (обычно "gpt-5.1")
- `OPENAI_MODEL_THINKING` - модель для перефразирования (например "gpt-5.1-thinking")
- `SQLITE_DB_FILENAME` - путь к локальной SQLite БД (legacy)
- `POSTGRES_DSN` - строка подключения к PostgreSQL
- `HUMANIZER_URL` - URL сервиса humanizer (gpthumanizer.io)
- Координаты для PyAutoGUI (SCROLL_COORDS, PASTE_COORDS, HUMANIZE_COORDS и т.д.)
- `LOG_LEVEL` - уровень логирования ("DEBUG", "INFO")
- `LOG_MODE` - режим логирования для scheduled_poster ("DEBUG" | "SUMMARY")
- `ALTERNATIVE_PROMPT_FREQUENCY` - частота использования альтернативного промпта (каждая N-я статья)

**Используется в:** Все модули проекта

---

#### `config_bot.py`
**Назначение:** Конфигурация Telegram бота
**Содержит:**
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `TELEGRAM_CHAT_ID` - ID чата для отправки уведомлений

**Используется в:** `telegram_bot.py`

---

### **ПРОМПТЫ ДЛЯ AI**

#### `prompts.py`
**Назначение:** Хранит все промпты для работы с AI моделями
**Содержит:**
- `prompt_1` - промпт для эксперта по убеждению (первый шаг генерации)
- `prompt_2` - инструкции для создания статей под разные платформы
- `prompt_3` - промпт для генерации SEO-оптимизированной статьи (использует {TOPIC HERE})
- `prompt_4` - промпт для структурирования статьи (стандартный, без ссылок)
- `prompt_4_links` - промпт для структурирования статьи (с встраиванием ссылки на bonza.chat)

**Используется в:** 
- `article_creation.py` (prompt_1, prompt_2, prompt_3)
- `humanizing.py` (prompt_4, prompt_4_links)

---

### **СОЗДАНИЕ СТАТЕЙ**

#### `article_creation.py`
**Назначение:** Генерация "сырых" статей через Anthropic Claude API
**Основной процесс:**
1. Запрашивает тему статьи и количество статей
2. Для каждой статьи выполняет 3 шага:
   - **Step 1:** Отправляет `prompt_1` → получает анализ темы
   - **Step 2:** Отправляет `prompt_2` → получает подготовку к написанию
   - **Step 3:** Отправляет `prompt_3` с темой → получает готовую статью
3. Парсит ответ (ищет "title:" и "body:")
4. Сохраняет в SQLite таблицу `raw_articles`

**Функции:**
- `get_claude_response()` - отправка запросов в Anthropic API
- `parse_article_text()` - парсинг title/body из ответа
- `insert_article()` - сохранение в SQLite
- `create_db_and_table()` - создание БД и таблицы

**Зависимости:**
- `prompts.py` (prompt_1, prompt_2, prompt_3)
- `config.py` (ANTHROPIC_API_KEY, MODEL_NAME - но в коде используется напрямую)

**Результат:** Статьи в SQLite таблице `raw_articles` (id, topic, title, body, created_at)

---

#### `humanizing.py`
**Назначение:** Обработка сырых статей: перефразирование, humanization, структурирование
**Основной процесс:**
1. Читает сырые статьи из PostgreSQL таблицы `raw_articles`
2. Для каждой статьи:
   - **Шаг 0:** Легкое перефразирование через OpenAI (модель thinking)
   - **Шаг 1:** Humanization через GUI (gpthumanizer.io) с PyAutoGUI:
     - Открывает браузер
     - Вставляет текст
     - Кликает кнопку "Humanize"
     - Копирует результат
   - **Шаг 2:** Структурирование через OpenAI:
     - Выбирает промпт (prompt_4 или prompt_4_links) в зависимости от `ALTERNATIVE_PROMPT_FREQUENCY`
     - Отправляет humanized текст + промпт
     - Парсит ответ (title, body, links, keywords, hashtags)
   - **Шаг 3:** Сохранение в PostgreSQL таблицу `refined_articles_<N>`

**Функции:**
- `rephrase_with_openai()` - перефразирование текста
- `process_article()` - полный цикл обработки одной статьи
- `parse_refined_response()` - парсинг структурированного ответа
- `insert_refined_article_postgres()` - сохранение в PostgreSQL
- `get_pg_conn()` - подключение к PostgreSQL
- `create_refined_table_postgres()` - создание таблицы refined_articles

**Зависимости:**
- `prompts.py` (prompt_4, prompt_4_links)
- `config.py` (OPENAI_API_KEY, OPENAI_MODEL, POSTGRES_DSN, HUMANIZER_URL, координаты)

**Результат:** Статьи в PostgreSQL таблице `refined_articles_<N>` с полями:
- id, topic, title, body, links, keywords
- hashtag1, hashtag2, hashtag3, hashtag4
- url (пустое до публикации), approval, is_link, created_at, profile_id

---

### **ПУБЛИКАЦИЯ СТАТЕЙ**

#### `medium_poster.py`
**Назначение:** Автоматическая публикация статей на Medium через PyAutoGUI и Ads Power
**Основной процесс:**
1. Подключается к PostgreSQL
2. Выбирает таблицу `refined_articles_*`
3. Выбирает статьи для публикации (по ID или все неопубликованные)
4. Для каждой статьи:
   - Открывает профиль Ads Power (через API)
   - Подключает Selenium к профилю
   - Открывает Medium new-story страницу
   - Через PyAutoGUI:
     - Вставляет заголовок
     - Вставляет тело статьи (как Rich Text HTML)
     - Кликает Publish
     - Вставляет хэштеги
     - Кликает финальный Publish
   - Получает URL опубликованной статьи
   - Обновляет БД (url, profile_id)

**Ключевые функции:**

**Работа с БД:**
- `get_pg_conn()` - подключение к PostgreSQL
- `get_refined_articles_tables()` - список таблиц refined_articles_*
- `get_articles_to_post()` - получение статей для публикации
- `update_article_url_and_profile()` - обновление URL и profile_id после публикации
- `ensure_profile_id_column()` - проверка/создание колонки profile_id

**Работа с профилями Ads Power:**
- `PROFILE_MAPPING` - словарь {profile_id: profile_no}
- `PROFILE_SEQUENTIAL_MAPPING` - словарь {profile_no: sequential_no} (1-10)
- `PROFILE_IDS` - список всех profile_id
- `check_ads_power_profile_status()` - проверка статуса профиля
- `open_ads_power_profile()` - открытие профиля (обертка)
- `open_and_maximize_profile()` - полная подготовка профиля
- `ensure_profile_ready()` - гарантия готовности профиля (старт через API, подключение Selenium)
- `close_profile()` - закрытие профиля через API
- `focus_profile_window()` - фокус и максимизация окна
- `minimize_profile_window()` - минимизация окна

**Работа с Medium:**
- `ensure_medium_tab_open()` - открытие вкладки Medium new-story
- `post_article_to_medium()` - полный цикл публикации статьи
- `markdown_to_html()` - конвертация Markdown в HTML
- `copy_markdown_as_rich_text()` - копирование как Rich Text (CF_HTML формат)

**Утилиты:**
- `random_delay()` - случайная задержка с вариацией
- `wait_with_log()` - ожидание с логированием
- `get_profile_no()`, `get_profile_id()`, `get_sequential_no()` - маппинг профилей

**Зависимости:**
- `config.py` (POSTGRES_DSN, LOG_LEVEL)
- `click_debug_screenshots.py` (capture_click_screenshot)

**Константы:**
- `MEDIUM_NEW_STORY_URL` = "https://medium.com/new-story"
- `ADS_POWER_API_URL` = "http://local.adspower.net:50325"
- Координаты для кликов (COORDS_TITLE_INPUT, COORDS_PUBLISH_BUTTON_1 и т.д.)
- Задержки (WAIT_AFTER_OPEN_TAB, WAIT_AFTER_TITLE_CLICK и т.д.)

**Результат:** Статьи опубликованы на Medium, URL сохранены в БД

---

#### `scheduled_poster.py`
**Назначение:** Автоматический постинг по расписанию с распределением по профилям
**Логика работы:**
- **Четные дни месяца** → профили 1-5 (sequential_no)
- **Нечетные дни месяца** → профили 6-10 (sequential_no)
- **Время постинга:** 19:30 - 20:20 GMT-5 (случайное для каждого профиля, минимум 10 минут между постами)
- **Распределение статей:**
  - 4 статьи с `is_link='no'`
  - 1 статья с `is_link='yes'`
  - Все 5 профилей должны постить (без пересечений)

**Основной процесс:**
1. Подключается к PostgreSQL
2. Выбирает таблицу `refined_articles_*`
3. Определяет профили для сегодня (`get_profiles_for_today()`)
4. Генерирует расписание постинга (`generate_posting_schedule()`)
5. Получает статьи из БД:
   - 4 статьи с `is_link='no'`
   - 1 статья с `is_link='yes'`
6. Распределяет статьи по профилям
7. Отправляет уведомление в Telegram о запуске
8. Для каждой статьи:
   - Ждет до времени постинга (`wait_until_time()`)
   - Открывает профиль
   - Публикует статью (использует функции из `medium_poster.py`)
   - Обновляет БД
   - Отправляет уведомление в Telegram
   - Закрывает профиль

**Функции:**
- `get_profiles_for_today()` - определение профилей для сегодня
- `generate_posting_schedule()` - генерация расписания с временами
- `wait_until_time()` - ожидание до указанного времени
- `get_articles_by_is_link()` - получение статей по фильтру is_link
- `log_summary()` - сокращенное логирование (режим SUMMARY)

**Зависимости:**
- `medium_poster.py` (все функции постинга)
- `telegram_bot.py` (notify_poster_started, notify_article_posted)
- `config.py` (LOG_LEVEL, LOG_MODE)

**Константы:**
- `GMT_MINUS_5` = pytz.timezone('America/New_York')
- `POSTING_START_HOUR` = 19.5 (19:30)
- `POSTING_END_HOUR` = 20.333 (20:20)
- `ARTICLES_NO_LINK_COUNT` = 4
- `ARTICLES_WITH_LINK_COUNT` = 1

---

### **УВЕДОМЛЕНИЯ**

#### `telegram_bot.py`
**Назначение:** Отправка уведомлений в Telegram о работе системы
**Функции:**
- `send_message()` - базовая отправка сообщения в Telegram
- `notify_poster_started()` - уведомление о запуске автопостера с расписанием
- `notify_article_posted()` - уведомление о публикации статьи (с заголовком, текстом, хэштегами, URL)

**Зависимости:**
- `config_bot.py` (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)

**Используется в:** `scheduled_poster.py`

---

### **МИГРАЦИЯ И УТИЛИТЫ**

#### `migrate_sqlite_to_postgres.py`
**Назначение:** Миграция данных из SQLite в PostgreSQL
**Процесс:**
1. Подключается к SQLite (articles.db)
2. Находит таблицы: `raw_articles`, `Refined_articles-*`, `refinedarticle`
3. Для каждой таблицы:
   - Создает соответствующую таблицу в PostgreSQL (если нет)
   - Копирует данные с `ON CONFLICT DO NOTHING`

**Функции:**
- `migrate_raw_articles()` - миграция raw_articles
- `migrate_refined_table()` - миграция refined таблиц
- `ensure_pg_table_raw()`, `ensure_pg_table_refined()` - создание таблиц

**Зависимости:**
- `config.py` (SQLITE_DB_FILENAME, POSTGRES_DSN)

---

#### `batch_docx_to_sqlite_via_openai.py`
**Назначение:** Обработка DOCX файлов и добавление меток title:/body: через OpenAI
**Процесс:**
1. Ищет все .docx файлы в текущей папке
2. Для каждого файла:
   - Читает текст из DOCX
   - Отправляет в OpenAI (Responses API) с промптом для добавления меток
   - Парсит ответ (ищет title: и body:)
   - Сохраняет в SQLite таблицу `raw_articles`

**Функции:**
- `read_docx_text()` - чтение текста из DOCX
- `call_openai_add_marks()` - отправка в OpenAI для добавления меток
- `extract_title_body()` - парсинг title/body

**Зависимости:**
- OpenAI API (через переменные окружения или хардкод)

---

#### `click_debug_screenshots.py`
**Назначение:** Утилита для отладки PyAutoGUI кликов (делает скриншоты с отметками координат)
**Функции:**
- `capture_click_screenshot()` - скриншот экрана с отметкой координат
- `click_with_debug()` - клик с автоматическим скриншотом до/после

**Используется в:** `medium_poster.py` (для отладки кликов)

---

#### `start_chrome_debug.py`
**Назначение:** Запуск Chrome с remote debugging (legacy, для старых версий проекта)
**Процесс:**
1. Закрывает все процессы Chrome
2. Проверяет доступность порта 9222
3. Запускает Chrome с `--remote-debugging-port=9222`
4. Проверяет, что debug endpoint отвечает

**Примечание:** В текущей версии используется Ads Power, этот скрипт может быть неактуален

---

### **ДОПОЛНИТЕЛЬНЫЕ ФАЙЛЫ**

#### `keywords.txt`
**Назначение:** Список SEO ключевых слов для использования в статьях
**Используется в:** `prompts.py` (встроен в промпты)

---

#### `requirements.txt`
**Назначение:** Список зависимостей Python
**Содержит:**
- openai, requests, pyautogui, pyperclip
- psycopg (PostgreSQL), pygetwindow, selenium
- markdown, pywin32, pytz

---

## 🔄 ПОТОК ДАННЫХ

### **Полный цикл создания и публикации статьи:**

```
1. article_creation.py
   └─> Генерация через Claude API
   └─> SQLite: raw_articles (topic, title, body)

2. migrate_sqlite_to_postgres.py (опционально)
   └─> Миграция raw_articles в PostgreSQL

3. humanizing.py
   └─> Чтение из PostgreSQL: raw_articles
   └─> Перефразирование (OpenAI thinking)
   └─> Humanization (gpthumanizer.io GUI)
   └─> Структурирование (OpenAI prompt_4/prompt_4_links)
   └─> PostgreSQL: refined_articles_<N> (полная структура)

4. medium_poster.py или scheduled_poster.py
   └─> Чтение из PostgreSQL: refined_articles_<N>
   └─> Публикация на Medium (PyAutoGUI + Ads Power)
   └─> Обновление БД: url, profile_id
```

---

## 🔗 ЗАВИСИМОСТИ МЕЖДУ МОДУЛЯМИ

```
config.py
├──> article_creation.py
├──> humanizing.py
├──> medium_poster.py
└──> scheduled_poster.py

config_bot.py
└──> telegram_bot.py

prompts.py
├──> article_creation.py (prompt_1, prompt_2, prompt_3)
└──> humanizing.py (prompt_4, prompt_4_links)

medium_poster.py
├──> scheduled_poster.py (импортирует все функции)
└──> click_debug_screenshots.py (capture_click_screenshot)

telegram_bot.py
└──> scheduled_poster.py (notify_poster_started, notify_article_posted)
```

---

## 📊 СТРУКТУРА БАЗЫ ДАННЫХ

### **PostgreSQL таблицы:**

#### `raw_articles`
- id (INTEGER, PRIMARY KEY)
- topic (TEXT)
- title (TEXT)
- body (TEXT)
- created_at (TEXT/TIMESTAMPTZ)

#### `refined_articles_<N>` (N - номер итерации)
- id (BIGSERIAL, PRIMARY KEY)
- topic (TEXT)
- title (TEXT)
- body (TEXT)
- links (TEXT)
- keywords (TEXT)
- hashtag1, hashtag2, hashtag3, hashtag4 (TEXT)
- hashtag5 (TEXT) - опционально
- url (TEXT) - заполняется после публикации
- approval (TEXT)
- is_link (TEXT) - "yes" или "no"
- created_at (TIMESTAMPTZ)
- profile_id (INTEGER) - заполняется после публикации

---

## 🎯 КЛЮЧЕВЫЕ КОНЦЕПЦИИ

### **Профили Ads Power:**
- **profile_id** - уникальный ID профиля в Ads Power (например "kqnfhbe")
- **profile_no** - внутренний номер профиля (например 70)
- **sequential_no** - порядковый номер 1-10 для ротации

**Маппинг:**
- `PROFILE_MAPPING`: {profile_id → profile_no}
- `PROFILE_SEQUENTIAL_MAPPING`: {profile_no → sequential_no}

### **Режимы работы:**
- **medium_poster.py** - ручной постинг (выбор статей, выбор профилей)
- **scheduled_poster.py** - автоматический постинг по расписанию с распределением

### **Типы статей:**
- **is_link='no'** - стандартная статья без встроенной ссылки
- **is_link='yes'** - статья с встроенной ссылкой на bonza.chat (использует prompt_4_links)

---

## 🛠️ ТЕХНОЛОГИИ

- **Python 3.x**
- **PostgreSQL** - основное хранилище
- **SQLite** - legacy хранилище (для миграции)
- **OpenAI API** - генерация и обработка контента
- **Anthropic Claude API** - генерация сырых статей
- **PyAutoGUI** - автоматизация браузера
- **Selenium** - управление браузером через Ads Power
- **Ads Power API** - управление профилями браузера
- **Telegram Bot API** - уведомления
- **gpthumanizer.io** - humanization текста (через GUI)

---

## 📝 ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Координаты PyAutoGUI** - зависят от разрешения экрана и браузера, могут требовать обновления
2. **Ads Power** - должен быть запущен локально на порту 50325
3. **Профили** - должны быть настроены в Ads Power с соответствующими profile_id
4. **Время постинга** - настроено на GMT-5 (America/New_York)
5. **Humanizer** - требует ручного взаимодействия через GUI (автоматизировано PyAutoGUI)
6. **Markdown форматирование** - статьи сохраняются с Markdown, конвертируются в HTML при публикации

---

## 🚀 ТИПИЧНЫЕ СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ

### **Создание новых статей:**
```bash
python article_creation.py
# Вводим тему и количество статей
# Результат: статьи в raw_articles
```

### **Обработка статей:**
```bash
python humanizing.py
# Выбираем ID статей из raw_articles
# Вводим номер итерации (N)
# Результат: статьи в refined_articles_N
```

### **Ручная публикация:**
```bash
python medium_poster.py
# Выбираем таблицу refined_articles_N
# Выбираем статьи (ID или все неопубликованные)
# Результат: статьи опубликованы на Medium
```

### **Автоматическая публикация по расписанию:**
```bash
python scheduled_poster.py
# Выбираем таблицу refined_articles_N
# Система автоматически:
#   - Определяет профили для сегодня
#   - Генерирует расписание
#   - Распределяет статьи
#   - Публикует по расписанию
```

---

## 🔍 ПОИСК ФУНКЦИЙ

**Работа с БД:**
- `get_pg_conn()` - medium_poster.py, humanizing.py
- `get_refined_articles_tables()` - medium_poster.py
- `get_articles_to_post()` - medium_poster.py
- `insert_refined_article_postgres()` - humanizing.py

**Работа с профилями:**
- `open_ads_power_profile()` - medium_poster.py
- `ensure_profile_ready()` - medium_poster.py
- `close_profile()` - medium_poster.py
- `focus_profile_window()` - medium_poster.py

**Публикация:**
- `post_article_to_medium()` - medium_poster.py
- `copy_markdown_as_rich_text()` - medium_poster.py

**Расписание:**
- `get_profiles_for_today()` - scheduled_poster.py
- `generate_posting_schedule()` - scheduled_poster.py
- `wait_until_time()` - scheduled_poster.py

---

**Последнее обновление:** 2025-01-XX
**Версия проекта:** 1.0

